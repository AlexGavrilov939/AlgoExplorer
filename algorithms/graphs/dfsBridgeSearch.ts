// Bridge - the edge which, when removed, makes the graph disconnected
// Find all bridges in the graph
// When the edge is a bridge? If there is no other way to reach top of the graph without the edge
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const dfsBridgeSearch = () => {};
