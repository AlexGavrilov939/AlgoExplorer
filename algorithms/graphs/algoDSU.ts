// unite(x, y) - unites two sets (set with element x and set with element y)
// find (x) - returns set with elements x (or set's representative)
// Main idea: store all the information using parent array, so all the elements can be represented as several trees.

// Disjoint-Set-Union
// eslint-disable-next-line @typescript-eslint/no-unused-vars
const algoDSU = () => {};
