// Complexity: V * extractMin() + E * decreaseKey()
// Usual array: extractMin() - O(V), decreaseKey() - O(1) => O(V^2 + E)
// Heap: extractMin() - O(logV), decreaseKey() - O(logV) => O((V + E) * logV)
// Advanced data structures: O(E + V * logV) or event O(E + V * log log V) for integer weights
const algoDijkstra = () => {};
