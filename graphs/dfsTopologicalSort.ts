// Find such vertex ordering in the graph that for every edge v->u comes before u
// After dfs from vertex v finished all vertices that are reachable from v are visited
// So we can just add v to the beginning of the current topological sort list
const dfsTopologicalSort = () => {};
