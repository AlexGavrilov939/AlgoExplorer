// Task: find n-th element in sorted array
// Example: array = [4, 5, 2, 9, 1], n = 3 => return 4
// Quick sort = choosing pivot + partition() + 2 recursive calls
// Complexity: O(n) average. Proof is identical to the one for quick sort
